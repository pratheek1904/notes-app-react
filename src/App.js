import React from 'react'
import './App.css'
import Form from './Form'

const App = () => {
  return (
    <div>
     {/* <Form Title={Title} setTitle={setTitle} Text={Text} setText={setText} Notes={Notes} setNotes={setNotes}/> */}
       <Form/>
    </div>
  )
}

export default App
